[package]
name = "intent-flux-bridge"
version = "0.1.0"
edition = "2021"
description = "Experiment: maps natural-language GPU intent to Flux bytecode. Tests the pincher→flux-core synergy where 'LLM as compiler' generates portable bytecode from intent."
license = "Apache-2.0"
repository = "https://github.com/SuperInstance/intent-flux-bridge"
keywords = ["intent", "flux", "gpu", "compiler", "pincher"]
categories = ["compilers"]
